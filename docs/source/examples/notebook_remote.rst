Remote notebook example
=======================

If you want to use a notebook `source code from a remote repository <https://docs.databricks.com/repos/jobs-remote-notebook.html>`_, you can also configure :code:`dbx` to deploy a workload with such settings.

.. literalinclude:: ./notebook_remote.yml
    :language: yaml
